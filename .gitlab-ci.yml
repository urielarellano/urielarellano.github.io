image: node:latest

variables:
  PUBLIC_URL: /cs409-final-project

cache:
  paths:
  - node_modules

stages:
  - deploy

pages:
  stage: deploy

  script:
    - npm install
    - npm run build
    - mv dist public

  artifacts:
    paths:
    - public

  only:
    - master
